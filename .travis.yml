sudo: required

language: java

services:
  - docker

after_success:
  - docker network create --driver bridge gw
  - docker pull mongo
  - docker run -d --name mongo-dbserver --network gw mongo
  - docker build -t gw .
  - docker run --network gw gw